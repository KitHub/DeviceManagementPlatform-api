log:
  log_level: "debug" # 日志级别（debug, info, warn, error）
  filename: "./logs/dmp-api.log" # 日志文件路径
  max_size: 100 # 文件最大尺寸（以MB为单位）
  max_backups: 3 # 保留的最大旧文件数量
  max_age: 30 # 保留旧文件的最大天数
  compress: true # 是否压缩/归档旧文件
  local_time: true # 使用本地时间创建时间戳